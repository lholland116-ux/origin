export interface CapaActionPlanAdvisoryResult { readonly advisory: { readonly proposal: { readonly advisory_summary: string; readonly completeness_linkage_concerns: readonly { readonly concern_key: string; readonly subject: string; readonly description: string; readonly reference_keys: readonly string[]; readonly human_review_question: string }[]; readonly action_improvements: readonly { readonly suggestion_key: string; readonly action_item_id: string | null; readonly issue: string; readonly recommendation: string; readonly rationale: string; readonly reference_keys: readonly string[]; readonly human_review_question: string }[]; readonly action_candidates: readonly { readonly suggestion_key: string; readonly action_type: "corrective" | "preventive" | "correction" | "containment"; readonly description: string; readonly deliverable: string; readonly implementation_evidence: string; readonly unintended_consequence_assessment: string; readonly linked_targets: readonly { readonly target_type: "cause" | "contributing_factor" | "gap"; readonly target_id: string; readonly rationale: string }[]; readonly effectiveness_planning: { readonly recommendation: string; readonly acceptance_criteria: string; readonly evaluation_method: string; readonly data_source: string; readonly timing: string; readonly responsible_role: string; readonly sample_or_rationale: string } | null; readonly reference_keys: readonly string[]; readonly human_review_question: string }[]; readonly effectiveness_planning_improvements: readonly { readonly suggestion_key: string; readonly action_item_id: string | null; readonly recommendation: string; readonly rationale: string; readonly reference_keys: readonly string[]; readonly human_review_question: string }[]; readonly proposed_action_plan: null } ; readonly warnings: readonly string[]; readonly uncertainty_and_limitations: readonly string[] }; readonly correlation_id: string; }
export type CapaActionPlanAdvisoryClientResult = { readonly status: "success"; readonly value: CapaActionPlanAdvisoryResult } | { readonly status: "failed"; readonly message: string; readonly correlation_id: string | null };
export async function generateActionPlanAdvisory(caseId: string, expectedCaseVersionId: string, expectedRecordVersion: number, fetcher: typeof fetch = fetch): Promise<CapaActionPlanAdvisoryClientResult> { try { const response = await fetcher(`/api/capa/${encodeURIComponent(caseId)}/action-plan-advisory`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ expected_case_version_id: expectedCaseVersionId, expected_record_version: expectedRecordVersion }), cache: "no-store" }); const body: unknown = await response.json().catch(() => null); if (!response.ok || typeof body !== "object" || body === null || !("advisory" in body) || !("correlation_id" in body) || typeof body.correlation_id !== "string") return { status: "failed", message: "The Action Planning advisory could not be generated.", correlation_id: typeof body === "object" && body !== null && "error" in body && typeof (body as { error?: { correlation_id?: unknown } }).error?.correlation_id === "string" ? (body as { error: { correlation_id: string } }).error.correlation_id : null }; return { status: "success", value: body as CapaActionPlanAdvisoryResult }; } catch { return { status: "failed", message: "The Action Planning advisory could not be generated.", correlation_id: null }; } }
